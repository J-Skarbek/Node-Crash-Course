const myName = "John A. Skarbek";

let interval = 0

console.log(myName);

const greet = input => console.log(`hello, ${input}`);

// greet(myName);

console.log(global);

setTimeout(() => {
  console.log('in the timeout');
  clearInterval(int)
}, 4000)


const int = setInterval(() => {
  console.log(interval += 1)
}, 1000)

